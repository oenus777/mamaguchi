<header>
    <nav class="navbar navbar-expand-sm navbar-dark mb-3 fixed-top" style="background-color:#eb616c;">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav4" aria-controls="navbarNav4" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <a class="navbar-brand" href="/">ママぐち</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/about">サイトについて</a>
                </li>
                <% if user_signed_in? %>
                <li class="nav-item">
                    <a class="nav-link" href="/posts">みんなの投稿</a>
                </li>
                <li class="nav-item">
                    <i class="fas fa-user"></i>
                    <%= link_to "#{current_user.name}", user_path(current_user), class: "nav-link" %>
                </li>
                <li class="nav-item">
                    <%= link_to "ログアウト", users_sign_out_path, method: :delete, class: "nav-link" %>
                </li>
                <% else %>
                <li class="nav-item">
                    <a class="nav-link" href="/login">ログイン</a>
                </li>
                <% end %>
            </ul>
            <% if user_signed_in? %>
            <%= render 'posts/search_form' %>
            <% end %>
        </div>
    </nav>
</header>

