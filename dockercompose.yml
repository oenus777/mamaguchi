version: "3"
services:
  app:
    build: .
    command: /bin/sh -c "rm -f tmp/pids/sever.pid && bundle exec rails s -p 3000 -b '0.0.0.0'"
    ports:
      - "3000:3000"
    restart: always
    depends_on:
      - db
    volumes:
      - .:/webapp
  db:
    image: mysql:5.7.28
